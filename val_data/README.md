# Example conditions

Place the example condition file(s) here, e.g. `A_SSZ_horiz.npz`, downloaded
from the data release. Each `.npz` must contain a sparse `horiz` array (in
[0,1]) and a `fault` skeleton array. See the top-level README for details.
